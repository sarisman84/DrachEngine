#include "precompile-header/coreIncludes.h"
#include "ProjectionInterface.h"

drach::ProjectionInterface::ProjectionInterface(const float aWidth, const float aHeight)
{
	myResolution = { aWidth, aHeight };
}

